name: Federated LLAMA Pre-Training
description: Federated pre-training of a LLAMA model.

environment:
  image:
    type: docker
    #gpu: jonassengaiml/opendiloco:latest  # Replace with your actual image
    cpu: jonassengaiml/opendiloco:latest

resources:
  slots: 0 # Number of GPUs; set to 0 for CPU-only training
  resource_pool: 42_Compute


entrypoint: null
